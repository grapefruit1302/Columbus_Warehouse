<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Форма майстра додавання товарів -->
        <record id="view_stock_transfer_add_items_wizard_form" model="ir.ui.view">
            <field name="name">stock.transfer.add.items.wizard.form</field>
            <field name="model">stock.transfer.add.items.wizard</field>
            <field name="arch" type="xml">
                <form string="Додавання товарів до переміщення">
                    <header>
                        <button name="action_confirm" string="Підтвердити" type="object" class="btn-primary"/>
                        <button name="action_cancel" string="Скасувати" type="object" class="btn-secondary"/>
                    </header>
                    
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <i class="fa fa-plus-circle text-primary"/> Додавання товарів
                            </h1>
                            <h2>
                                Документ: <field name="transfer_id" readonly="1" nolabel="1"/>
                            </h2>
                        </div>
                        
                        <!-- Інформаційна панель -->
                        <group col="2">
                            <group string="📊 Статистика">
                                <field name="existing_lines_count" readonly="1"/>
                                <field name="available_items_count" readonly="1"/>
                            </group>
                            
                            <group string="⚙️ Налаштування">
                                <field name="action_type" widget="radio"/>
                            </group>
                        </group>
                        
                        <!-- Попередження -->
                        <div class="alert alert-warning" role="alert" invisible="not warning_message">
                            <field name="warning_message" readonly="1" nolabel="1"/>
                        </div>
                        
                        <!-- Список товарів для вибіркового додавання -->
                        <notebook invisible="action_type != 'add_selected'">
                            <page string="🛒 Вибір товарів для додавання">
                                <field name="item_ids">
                                    <list string="Доступні товари" editable="bottom">
                                        <field name="selected" widget="boolean_toggle"/>
                                        <field name="nomenclature_id" readonly="1"/>
                                        <field name="lot_batch" readonly="1"/>
                                        <field name="available_qty" readonly="1" decoration-info="available_qty > 0"/>
                                        <field name="qty_to_add" invisible="not selected" decoration-danger="qty_to_add > available_qty"/>
                                        <field name="already_exists" readonly="1" widget="boolean_toggle"/>
                                        <field name="existing_qty" readonly="1" invisible="not already_exists" decoration-muted="already_exists"/>
                                    </list>
                                </field>
                                
                                <!-- Кнопки швидких дій -->
                                <div class="mt-3">
                                    <button name="action_select_all" string="✅ Вибрати всі" type="object" class="btn-link"/>
                                    <button name="action_deselect_all" string="❌ Зняти вибір" type="object" class="btn-link"/>
                                    <button name="action_set_max_qty_all" string="📈 Макс. кількість для всіх" type="object" class="btn-link"/>
                                    <button name="action_clear_qty_all" string="🧹 Очистити кількості" type="object" class="btn-link"/>
                                </div>
                                
                                <!-- Підказки -->
                                <div class="alert alert-info mt-3" role="alert">
                                    <h6><i class="fa fa-lightbulb-o"/> 💡 Підказки:</h6>
                                    <ul class="mb-0">
                                        <li>✅ <strong>Зелена галочка</strong> - товар буде додано</li>
                                        <li>🔄 <strong>"Вже існує"</strong> - товар уже є в документі, кількість буде додана</li>
                                        <li>🔴 <strong>Червоний текст</strong> - кількість перевищує доступну</li>
                                        <li>⚡ Використовуйте кнопки швидких дій для масових операцій</li>
                                    </ul>
                                </div>
                            </page>
                        </notebook>
                        
                        <!-- Підсумкова інформація для автоматичних дій -->
                        <div invisible="action_type == 'add_selected'">
                            <group string="📋 Деталі операції" col="1">
                                <div class="alert alert-info" role="alert">
                                    <h6>ℹ️ Інформація про операцію:</h6>
                                    <ul class="mb-0">
                                        <li invisible="action_type != 'add_all'">
                                            <strong>Додавання всіх товарів:</strong> всі <field name="available_items_count" nolabel="1" readonly="1"/> 
                                            доступних товарів будуть додані до документа
                                        </li>
                                        <li invisible="action_type != 'replace_all'">
                                            <strong>Заміна всіх позицій:</strong> <field name="existing_lines_count" nolabel="1" readonly="1"/> 
                                            існуючих позицій будуть видалені та замінені на 
                                            <field name="available_items_count" nolabel="1" readonly="1"/> нових
                                        </li>
                                    </ul>
                                </div>
                            </group>
                        </div>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action для майстра -->
        <record id="action_stock_transfer_add_items_wizard" model="ir.actions.act_window">
            <field name="name">Додавання товарів до переміщення</field>
            <field name="res_model">stock.transfer.add.items.wizard</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_stock_transfer_add_items_wizard_form"/>
            <field name="target">new</field>
        </record>

    </data>
</odoo>