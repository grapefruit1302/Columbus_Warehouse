<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Actions для нових візардів -->
        <record id="action_stock_balance_serial_detailed_wizard" model="ir.actions.act_window">
            <field name="name">Детальний перегляд серійних номерів</field>
            <field name="res_model">stock.balance.serial.detailed.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

        <record id="action_stock_balance_serial_management_wizard" model="ir.actions.act_window">
            <field name="name">Управління серійними номерами</field>
            <field name="res_model">stock.balance.serial.management.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

        <record id="action_stock_balance_serial_import_wizard" model="ir.actions.act_window">
            <field name="name">Імпорт серійних номерів</field>
            <field name="res_model">stock.balance.serial.import.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

        <!-- Меню для швидкого доступу до проблемних залишків -->
        <record id="action_incomplete_serial_balances" model="ir.actions.act_window">
            <field name="name">Неповні серійні номери</field>
            <field name="res_model">stock.balance</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('has_serial_tracking', '=', True), ('missing_serial_count', '>', 0)]</field>
            <field name="context">{'search_default_incomplete_serials': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Відмінно! Всі товари з серійним обліком мають повні серійні номери.
                </p>
                <p>
                    Тут відображаються залишки товарів з серійним обліком, 
                    у яких є фізична кількість, але не всі серійні номери введені.
                </p>
            </field>
        </record>

        <!-- Додаємо меню для проблемних залишків -->
        <menuitem id="menu_incomplete_serial_balances" 
                  name="⚠️ Неповні серійні номери" 
                  parent="menu_stock_balance_management" 
                  action="action_incomplete_serial_balances" 
                  sequence="35"/>

        <!-- Звіт по всіх серійних номерах -->
        <record id="action_all_serial_numbers_report" model="ir.actions.act_window">
            <field name="name">Всі серійні номери в системі</field>
            <field name="res_model">stock.serial.report</field>
            <field name="view_mode">list</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Поки що немає серійних номерів в системі!
                </p>
                <p>
                    Тут відображаються всі серійні номери по всіх товарах та локаціях.
                    Корисно для пошуку конкретного серійного номера.
                </p>
            </field>
        </record>

        <!-- Оновлюємо існуюче меню для звіту серійних номерів -->
        <menuitem id="menu_stock_serial_report_updated" 
                  name="🔍 Всі серійні номери" 
                  parent="menu_stock_balance_management" 
                  action="action_all_serial_numbers_report" 
                  sequence="41"/>
    </data>
</odoo>